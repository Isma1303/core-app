import { RoleService, TablesService } from '../../services'

const tableService = new TablesService()
const rolesService = new RoleService()

export const TablesToRoles = () => {
    return <div>TablesToRoles</div>
}
